/* macros can replace certain variables in game
ie: #macro SECONDS 60 means every instance of
SECONDS in the code will be set to 60
useful for global constant variables
can use functions, values, strings, t/f booleans
*/

